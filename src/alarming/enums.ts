export enum AlarmPriority {
  low,
  middle,
  high
}

export enum AlarmDirection {
  came = 'came',
  gone = 'gone',
}

export enum AlarmType {
  Unknown = 'Unknown',
  Normal = 'Normal',
  PreWarning = 'PreWarning',
  Warning = 'Warning',
  Alarm = 'Alarm',
}
