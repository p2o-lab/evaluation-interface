export enum TopologyObjectType {
  Junction,
  Sink,
  Source,
  Termination
}

export enum TopologyConnectionType {
  Function,
  Measurement,
  Pipe
}

export enum PortType {
  Logic,
  Measurement,
  Nozzle
}
